/**
 * The text (ASCII) render engine: a native {@code RenderEngine} mapping a laid-out scene onto a
 * character grid for terminal output. A leaf engine depending only on {@code contracts} and {@code
 * engine-api}.
 */
package dev.dediren.plugins.asciirender;
