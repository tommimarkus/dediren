/**
 * The draw.io (mxGraph/`.drawio`) lane: both a native {@code ExportEngine} and a native {@code
 * ImportEngine} publishing engine id {@code "drawio"} under their respective capabilities. Depends
 * only on {@code contracts} and {@code engine-api} — a leaf engine, like {@code mermaid-import} and
 * {@code dot-import}, with no notation-core or schema-cache edge.
 */
package dev.dediren.plugins.drawio;
