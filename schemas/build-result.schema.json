{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dediren.dev/schemas/build-result.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["build_result_schema_version", "status", "views"],
  "properties": {
    "build_result_schema_version": { "const": "build-result.schema.v1" },
    "status": { "enum": ["ok", "warning", "error"] },
    "views": {
      "type": "array",
      "items": { "$ref": "#/$defs/viewOutcome" }
    },
    "diagnostics": {
      "type": "array",
      "items": { "$ref": "#/$defs/diagnostic" }
    }
  },
  "$defs": {
    "viewOutcome": {
      "type": "object",
      "additionalProperties": false,
      "required": ["view_id", "status", "artifacts", "diagnostics"],
      "properties": {
        "view_id": { "type": "string", "minLength": 1 },
        "status": { "enum": ["ok", "warning", "error"] },
        "artifacts": {
          "type": "array",
          "items": { "$ref": "#/$defs/artifact" }
        },
        "diagnostics": {
          "type": "array",
          "items": { "$ref": "#/$defs/diagnostic" }
        }
      }
    },
    "artifact": {
      "type": "object",
      "additionalProperties": false,
      "required": ["artifact_kind", "path"],
      "properties": {
        "artifact_kind": { "type": "string", "minLength": 1 },
        "path": { "type": "string", "minLength": 1 }
      }
    },
    "diagnostic": {
      "type": "object",
      "additionalProperties": false,
      "required": ["code", "severity", "message"],
      "properties": {
        "code": { "type": "string", "minLength": 1 },
        "severity": { "enum": ["info", "warning", "error"] },
        "message": { "type": "string" },
        "path": { "type": "string" },
        "source_pointer": { "type": "string", "pattern": "^/" },
        "migration": { "$ref": "#/$defs/migration" }
      }
    },
    "migration": {
      "type": "object",
      "additionalProperties": false,
      "required": ["from", "to", "operations"],
      "properties": {
        "from": { "type": "string" },
        "to": { "type": "string" },
        "operations": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["op"],
            "properties": {
              "op": { "enum": ["rename_field", "remove_key", "set_version", "regenerate"] },
              "pointer": { "type": "string", "pattern": "^/" },
              "to": { "type": "string", "pattern": "^/" },
              "value": { "type": "string" }
            }
          }
        }
      }
    }
  }
}
